// This could service multiple different RPCs, so it's named generically.
pub struct ServerState {
    // TODO: Handle into IDP DB
}

impl ServerState {
    pub fn new() -> ServerState {
        ServerState { }
    }
}
